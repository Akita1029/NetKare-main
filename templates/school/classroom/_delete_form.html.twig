<form method="post" action="{{ path('school_classroom_delete', {'id': classroom.id}) }}" onsubmit="return confirm('{% trans %}buttons.delete.confirm{% endtrans %}');" class="inline-block">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ classroom.id) }}">
    <button class="btn btn-danger">{% trans %}buttons.delete.label{% endtrans %}</button>
</form>
